const Loader = () => {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        height: "100vh",
        background: "#383f42",
      }}
    >
      <img src="./loader1.gif" alt="logo" width={30} />
    </div>
  );
};

export default Loader;
