const ErrorMessage = () => {
  return (
    <div className="error-message">
      <span>⛔</span>
      <p>There was an error fetching the questions.</p>
    </div>
  );
};

export default ErrorMessage;
